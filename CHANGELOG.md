# 更新日志 (Changelog)

## v0.3 (2026-03-09)
### ✨ 新增功能 (New Features)
- **⚙️ 特殊规则管理 (Special Rules)**:
  - 新增专用管理页面 (`/rules`)，支持通过界面进行 B0M 匹配规则的增删改查。
  - **导入导出**：支持导出全部或单条规则为 JSON 文件，并支持文件上传导入（合并/覆盖）。
  - **可视化编辑**：内置 JSON 编辑器，支持语法验证，包含“智能匹配”开关。
  - **Pretty Print**：自动格式化存储的规则文件，中文字符直接显示（非 ASCII 转义），提升可读性。
- **⏰ UTC 时间支持**:
  - 后端统一使用 UTC 存储所有时间记录（上传时间、库存变动历史等）。
  - 前端自动根据用户浏览器时区将 UTC 时间转换为本地可读格式。
- **🔧 界面优化**:
  - **侧边栏 (Sidebar)**：改为固定定位 (Fixed)，即使页面滚动也能始终显示，并优化了折叠后的布局适配。
  - **按钮样式**：补充了 `.btn-info` 样式，修复导出按钮在部分背景下不可见的问题。

### 🐞 修复与改进 (Fixes & Improvements)
- 修复了文件上传时可能出现的重名问题，现在文件名添加 UTC 时间戳以确保唯一性。
- 优化了规则编辑器中的 JSON 显示，支持中文直接显示。

## v0.2 (2026-03-08)
### ✨ 新增功能 (New Features)
- **💰 费用统计系统**：
  - 新增单价与总价记录。
  - 支持多币种（CNY / USD / EUR）显示与汇率换算配置。
  - 自动计算库存总价值。
- **🖥️ 界面与交互优化**：
  - **Toast 提示框**：操作成功或失败时，顶部弹出带有动画的浮动提示框，自动消失，体验更流畅。
  - **无刷新编辑**：库存编辑模态框支持 AJAX 提交，保存后直接更新表格数据，无需刷新页面。
  - **侧边栏升级**：支持点击折叠/展开，并自动记忆用户的使用习惯；优化了最小宽度显示。
  - **拖拽上传**：CSV 管理页面新增文件的拖拽上传 (Drag & Drop) 功能。
- **🎨 图标美化**：
  - 全面引入 FontAwesome 图标库，替换了原有的纯文本标签，界面更加现代直观。
- **⚙️ 设置优化**：
  - 将语言切换功能从侧边栏移动至独立的“设置”页面，布局更加合理。

### 🐞 修复与改进 (Fixes & Improvements)
- 优化了移动端和不同屏幕尺寸下的布局表现。
- 统一了提示信息的风格。

---

## v0.1 (2026-03-07)
### 🎉 初始发布 (Initial Release)
- **基础库存管理**：支持电子元件的添加、编辑、删除、搜索。
- **多级分类系统**：支持主分类与次级分类的创建与管理。
- **BOM 管理**：
  - 支持 CSV 格式 BOM 文件的上传与解析。
  - 自动根据型号/封装匹配库存。
  - 支持一键“出库”操作，扣减相应库存。
- **多语言支持**：内置中文、英语、德语三种语言界面。
- **本地化部署**：无需外部网络依赖，静态资源全本地化。
